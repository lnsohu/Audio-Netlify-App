{
  "name": "my-recording-app",
  "version": "1.0.0",
  "description": "A simple recording web app for Netlify",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "busboy": "^1.6.0"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0"
  }
}
